# Citizen Complaint Portal — Frontend

This is a minimal React frontend for the Citizen Complaint Portal backend located in `../backend`.

Prerequisites:
- Node.js 18+ and npm

Install and run:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend to run on `http://localhost:3000` and will include credentials (cookies) in requests.
